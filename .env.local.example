# Convex
NEXT_PUBLIC_CONVEX_URL=your_convex_deployment_url
CONVEX_DEPLOY_KEY=your_convex_deploy_key

# To get these values:
# 1. Run `npx convex dev` to create a deployment
# 2. Copy the URL from the terminal output
